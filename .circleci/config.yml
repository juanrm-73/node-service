version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10.22.0
    steps:
      - checkout
      - run: npm install
      - run: npm run test

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build